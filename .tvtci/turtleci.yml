version: 1
name: "Mobile Build Workflow"
on:
  push:
    branches:
      - main
      - master

jobs:
  test_connection:
    name: "Validate GitHub Hook"
    builder: ubuntu-22.04         # <--- TURTLECI REQUIRES THIS CRITICAL LINE
    steps:
      - name: "Say Hello"
        run: echo "TurtleCI successfully connected to GitHub!"
